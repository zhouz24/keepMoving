1、代理模式：为一个对象提供一个替身，已控制对这个对象的访问，即通过代理对象范文目标对象。
    这样做的好处是：可以在目标对象实现的将基础上，增强额外的功能操作，即扩展目标对象的功能。
2、被代理的对象可以是：远程对象，创建开销大的对象，或需要安全控制的对象。
3、代理有三种不同的形式：静态代理、动态代理（jdk代理，接口代理）、cglib代理（可以在内存动态的创建对象，而不需要实现接口，也属于动态代理的范畴）


